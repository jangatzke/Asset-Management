# Prisma CLI fallback used when validating this schema from the workspace root.
# Keep this as a non-secret placeholder; runtime configuration belongs in ../.env.
DATABASE_URL="postgresql://postgres:password@localhost:5432/asset_management?schema=public"
